import React from "react";
import { Rule } from "../../core/types";
import { Editor } from "react-flow-editor";

export function RuleDiagram(props: { rule: Rule }) {
  return <Editor config={XXX} nodes={XXX} />;
}
