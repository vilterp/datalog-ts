import { RuleGraph } from "./types";
import { Rec } from "../types";

function assertStep(
  graph: RuleGraph,
  rec: Rec
): { newGraph: RuleGraph; newFacts: Rec[] } {
  throw new Error("TODO");
}

// TODO: retractStep
