main {
  lock = prim.newLock();
  block.acquireLock(lock);
  forkTo afterFork;
  t = <<5>>;
  block.sleep(t);
  releaseLock(lock);
  goto end;
}
afterFork {
  block.acquireLock(lock);
  x5 = 10;
}
end {}
