countUp {
  x = 0;
  goto loop;
}
loop {
  forked = fork();
  goto afterFork if forked;
  x = base.incr(x);
  threshold = 5;
  going = base.lt(x, threshold);
  goto loop if going;
}
afterFork {
  done = 42;
}
