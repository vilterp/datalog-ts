main {
  lock1 = prim.newLock();
  lock2 = prim.newLock();
  block.acquireLock(lock1);
  forkTo afterFork;
  t = <<5>>;
  block.sleep(t);
  block.acquireLock(lock2);
}
afterFork {
  t2 = <<1>>;
  block.sleep(t2);
  block.acquireLock(lock2);
  block.acquireLock(lock1);
}
end {}
